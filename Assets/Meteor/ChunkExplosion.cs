using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChunkExplosion : MonoBehaviour
{
    public float Value;
    // Start is called before the first frame update
    void Start()
    {
        CameraShake.QuickShake(2);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
